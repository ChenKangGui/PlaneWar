#include "snack.h"

Snack::Snack(QMainWindow *parent)
{

}

Snack::~Snack()
{

}


/*
void Snack::up_run(Snack *h, Dessert *dst)
{
    QPushButton *btn = new QPushButton("btn", this->parent);
    btn->setFixedSize(50, 50);
    btn->move(0,0);


}

void Snack::down_run(Snack *h, Dessert *dst)
{
    QPushButton *btn = new QPushButton("btn", parent);
    btn->setFixedSize(50, 50);
    btn->move(0,0);

    Snack *p = h->next;

    while(p->next->next != NULL)
    {
        p = p->next;
    }
    Snack *last = p->next;
    p->next = NULL;


}

void Snack::right_run(Snack *h, Dessert *dst)
{
    QPushButton *btn = new QPushButton("btn", parent);
    btn->setFixedSize(50, 50);
    btn->move(0,0);

    Snack *p = h->next;

    while(p->next->next != NULL)
    {
        p = p->next;
    }
    Snack *last = p->next;
    p->next = NULL;


}

void Snack::left_run(Snack *h, Dessert *dst)
{
    QPushButton *btn = new QPushButton("btn", parent);
    btn->setFixedSize(50, 50);
    btn->move(0,0);

    Snack *p = h->next;

    while(p->next->next != NULL)
    {
        p = p->next;
    }
    Snack *last = p->next;
    p->next = NULL;


}
*/


